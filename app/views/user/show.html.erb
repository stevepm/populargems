<div class="container">
  <div class="row">
    <div class="col-md-8">
      <table class="table table-striped table-hover well">
        <thead>
        <tr>
          <th>Comments</th>
        </tr>
        </thead>
        <tbody>
        <% @comments.each do |comment| %>
          <tr class="success">
            <td><strong><%= link_to comment.popular_gem.name, comment.popular_gem %></strong></td>
            <td><%= comment.body.html_safe %></td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>

    <div class="col-md-offset-1 col-md-3" id="loved_gems">
      <table class="table table-striped table-hover well">
        <thead>
        <tr>
          <th>Loved Gems</th>
        </tr>
        </thead>
        <tbody>
        <% @loved_gems.each do |gem| %>
          <tr class="info">
            <td><strong>
              <%= link_to gem.name, gem %>
            </strong></td>
            <td>
              <%= render partial: 'shared/heart_button', locals: {gem: gem} %>
              <%= render partial: 'shared/gem_likes_label', locals: {gem: gem} %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>